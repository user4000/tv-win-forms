using Telerik.WinControls.UI;

namespace TvWinForms
{
  public enum MsgPos
  {
    BottomRight = AlertScreenPosition.BottomRight,
    BottomLeft = AlertScreenPosition.BottomLeft,
    BottomCenter = AlertScreenPosition.BottomCenter,
    TopRight = AlertScreenPosition.TopRight,
    TopLeft = AlertScreenPosition.TopLeft,
    TopCenter = AlertScreenPosition.TopCenter,
    Manual = AlertScreenPosition.Manual,
    ScreenCenter = 999001, // Screen center //
    Unknown = 999002  // Unknown //
  }
}